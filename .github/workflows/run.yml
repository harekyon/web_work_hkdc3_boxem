name: learn-github-actions
on: [push]
env:
  ENVTEST: ${{secrets.ENVTESTa}}
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    environment: ${{ inputs.deployment_target }}
    steps:
      - run: echo "ths is env=${{ env.ENVTEST }}"
